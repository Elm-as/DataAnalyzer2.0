# Railway (Nixpacks) build plan

[start]
cmd = "python manage.py migrate --noinput && gunicorn modules.dataanalyzer.wsgi:application --bind 0.0.0.0:${PORT}"

[phases.build]
cmds = [
  "python manage.py collectstatic --noinput"
]
